[project]
name = "small_concept_model"
version = "0.1.0"
description = "Small concept model implementation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "protobuf",
    "scikit-learn",
    "sentencepiece",
    "spacy",
    "torch",
    "transformers",
]
